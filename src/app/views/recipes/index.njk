{% extends 'layout.njk' %}

{% block content %}
<section id="recipes" class="container">
    <div class="recipes-grid grid">
        {% for recipe in recipes %}
        <a href="/receitas/{{loop.index0}}">
        <div class="recipe">
            <img src="{{recipe.image}}" alt="Imagem de {{recipe.title}}">
            <div class="info">
                <div class="name">{{recipe.title}}</div>
                <div class="author">por {{recipe.author}}</div>
            </div>
        </div>
        </a>
        {% endfor %}
        {% for recipe in recipes %}
        <a href="/receitas/{{loop.index0}}">
        <div class="recipe">
            <img src="{{recipe.image}}" alt="Imagem de {{recipe.title}}">
            <div class="info">
                <div class="name">{{recipe.title}}</div>
                <div class="author">por {{recipe.author}}</div>
            </div>
        </div>
        </a>
        {% endfor %}
    </div>
</section>
{% endblock content %}